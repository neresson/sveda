apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sveda-server.fullname" . }}
  labels:
    {{- include "sveda-server.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
