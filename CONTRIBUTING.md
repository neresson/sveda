# Contributing

1. Run `pnpm install` at the monorepo root.
2. Run `pnpm test` and `pnpm typecheck` before submitting changes.
3. For Laravel changes, run tests in `laravel/veda-laravel`.
4. Use changesets for user-facing package changes.
